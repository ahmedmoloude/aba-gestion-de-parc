import { Action, createReducer, on } from '@ngrx/store';


export const receptionChequeTraiteFeatureKey = 'receptionChequeTraite';

export interface receptionChequeTraiteState {

}

export const initialState: receptionChequeTraiteState = {

};


export const receptionChequeTraiteReducer = createReducer(
  initialState,

);

